#include <stdio.h>
#include "main.h"

/**
* mul - multiply two digits
* @a : the first digit to be multiplied
* @b : the second digit to be multiplied
* Return: the result of multiplication
*/
int mul(int a, int b)
{
int result;

result = a * b;
return (result);
}
